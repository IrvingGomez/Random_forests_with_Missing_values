library(scales)
detach("package:scales", unload = TRUE)
library(RColorBrewer)
detach("package:RColorBrewer", unload = TRUE)
dat <- iris
dat
dat[with(dat, order(dat$Sepal.Length)),]
dat[with(dat, order(-dat$Sepal.Length)),]
